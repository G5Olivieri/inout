version: "3"

services:
  app:
    image: node:14-alpine
    command: yarn dev
    working_dir: /app
    environment:
      HOSTNAME: host.docker.internal
    ports:
      - 3000:3000
    volumes:
      - .:/app
    extra_hosts:
      - "host.docker.internal:host-gateway"
